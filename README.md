# Webir2025
Primero instalar todo lo que esta en requisitos.txt
 -> pip install requirements.txt

Para ejecutar:
-> python app.py
Luego dirigirse a: http://localhost:5000. La app queda levantada localmente.
